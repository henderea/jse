{
  "name": "@henderea/jse",
  "version": "0.7.8",
  "description": "JavaScript Executor",
  "main": "dist/api/index.mjs",
  "types": "types.d.ts",
  "repository": "git@github.com:henderea/jse.git",
  "author": "Eric Henderson <henderea@gmail.com>",
  "license": "MIT",
  "engines": {
    "node": ">= 18"
  },
  "engineStrict": true,
  "scripts": {
    "ncc:build": "yarn ncc:build:release",
    "ncc:clean": "rm -rf dist",
    "ncc:post-clean": "rm -rf dist/**/*.hbs && rm -rf dist/**/locales",
    "ncc:build:release:build:cli": "ncc build src/jse.mjs -m -q -o dist/cli",
    "ncc:build:debug:build:cli": "ncc build src/jse.mjs -q -o dist/cli",
    "ncc:build:release:build:api": "ncc build src/api.mjs -m -q -o dist/api",
    "ncc:build:debug:build:api": "ncc build src/api.mjs -q -o dist/api",
    "ncc:build:release": "yarn ncc:clean && yarn ncc:build:release:build:cli && yarn ncc:build:release:build:api && yarn ncc:post-clean",
    "ncc:build:debug": "yarn ncc:clean && yarn ncc:build:debug:build:cli && yarn ncc:build:debug:build:api && yarn ncc:post-clean",
    "prepublishOnly": "yarn ncc:build",
    "lint": "eslint --ext .js,.mjs bin src lib",
    "lint:fix": "eslint --ext .js,.mjs --fix bin src lib"
  },
  "bin": {
    "jse": "bin/jse.mjs"
  },
  "devDependencies": {
    "@henderea/simple-colors": "^2.0.6",
    "@typescript-eslint/eslint-plugin": "^6.7.3",
    "@typescript-eslint/parser": "^6.7.3",
    "@vercel/ncc": "^0.38.0",
    "any-shell-escape": "^0.1.1",
    "escalade": "^3.1.1",
    "eslint": "^8.50.0",
    "eslint-config-henderea": "1.1.16",
    "eslint-plugin-import": "^2.28.1",
    "lodash": "^4.17.21",
    "natural-orderby": "^3.0.2",
    "typescript": "^5.2.2",
    "yargs": "17.7.2"
  },
  "files": [
    "bin/**/*",
    "dist/**/*",
    "resources/**/*"
  ],
  "eslintConfig": {
    "extends": "henderea",
    "ignorePatterns": "**/*.ts"
  }
}
